import react from "react";
import NoteContext from "./NoteContext";

const NoteState = () => {
  
    return (
        <NoteState.Porvider value = {[]}>
            {props.children}
        </NoteState.Porvider>
    )
}
export default NoteState;